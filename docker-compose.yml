version: '3.8'
services:
  mcp-server:
    build: .
    container_name: mcp-server
    ports:
      - "8945:8000"
    env_file:
      - .env
    volumes:
      - ./app:/app
    networks:
      - mcp-network

networks:
  mcp-network:
    driver: bridge
